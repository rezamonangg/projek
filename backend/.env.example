# Environment (development, production)
ENV=development

# Server
PORT=8080

# Database
DB_HOST=localhost
DB_PORT=5432
DB_USER=projek
DB_PASSWORD=projek
DB_NAME=projek

# Redis
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_PASSWORD=

# Auth
SESSION_TTL=168h
PASSWORD_MIN_LEN=8

# Email (SMTP)
SMTP_HOST=localhost
SMTP_PORT=587
SMTP_USER=
SMTP_PASSWORD=
FROM_EMAIL=noreply@projek.local
FROM_NAME=Projek
