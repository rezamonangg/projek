global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  - job_name: 'projek-backend'
    static_configs:
      - targets: ['api:8080']
        labels:
          service: 'projek-backend'
          env: 'production'
